方案设计.md

一、功能需求分析

1.1 业务背景

在机械加工过程中，刀具的磨损状态直接影响工件加工质量与生产效率。切削速度、进给量、切削深度等工艺参数设置不当会加速刀具磨损，导致工件表面粗糙度增加、尺寸超差甚至工件报废。目前，多数车间仍依赖操作人员经验判断刀具磨损程度并决定换刀时机，这种人工判断方式存在主观性强、标准不统一、响应不及时等问题。本系统针对这一业务痛点，运用人工智能方法建立切削参数与刀具磨损之间的量化关系模型，实现刀具状态的智能评估与换刀辅助决策。

1.2 用户角色

本系统面向以下两类用户：
（1）操作人员/工艺员：输入当前加工的切削参数，查看刀具磨损评估结果与换刀建议。
（2）车间管理人员：查看历史评估记录与统计报表，了解刀具整体使用情况。

1.3 功能需求

（1）参数输入：支持用户输入切削速度（m/min）、进给量（mm/r）、切削深度（mm）、振动值（mm/s）四项工艺参数，优先级高。
（2）磨损等级评估：根据输入的工艺参数，输出刀具磨损等级（轻度磨损/中度磨损/重度磨损），优先级高。
（3）剩余寿命预测：输出刀具剩余使用寿命百分比（0-100%），优先级高。
（4）换刀建议：综合磨损等级与剩余寿命，给出"建议换刀"或"可继续使用"的决策建议，优先级高。
（5）历史记录查询：查看所有历史评估记录，支持按时间倒序排列，优先级中。
（6）数据可视化展示：通过图表展示磨损等级分布统计、剩余寿命变化趋势，优先级中。

1.4 非功能需求

（1）单次预测响应时间不超过2秒。
（2）前端界面简洁清晰，符合工业软件风格，适合车间使用场景。
（3）系统采用B/S架构，用户通过浏览器访问，无需安装客户端。
（4）系统具备基本的容错能力，对异常输入给出友好提示。

二、方案论证

2.1 技术选型论证

前端技术选型：采用HTML + CSS + JavaScript + ECharts组合。该方案轻量级，无需学习前端框架，开发周期短；ECharts提供丰富的工业图表类型（饼图、折线图、雷达图等），满足数据可视化需求。

后端技术选型：采用Python + Flask框架。Flask轻量灵活，与scikit-learn机器学习库无缝集成，便于算法模块的调用与部署。

数据库选型：采用SQLite。轻量级文件型数据库，无需独立部署数据库服务，适合课程设计Demo阶段使用，后续可平滑迁移至MySQL。

算法库选型：采用scikit-learn、pandas、numpy。均为课程学习中使用的标准库，覆盖数据预处理、分类、回归等全流程。

AI开发工具选型：采用通义灵码（Harness）搭配Qwen 3.8模型，符合课程vibecoding要求，IDE插件可读写项目文件，辅助完成代码生成与调试。

2.2 算法方案论证

磨损等级分类（监督学习-多分类）：
本问题为典型的多分类任务，需将刀具磨损状态划分为轻度、中度、重度三个等级。候选算法包括决策树、随机森林、逻辑回归。综合考虑准确率、泛化能力和可解释性，初步选用随机森林算法。随机森林通过集成多棵决策树的结果进行分类，抗过拟合能力强，且能输出特征重要性，便于分析各工艺参数对磨损的影响程度。评估指标采用准确率（Accuracy）和宏平均F1-Score。

剩余寿命预测（监督学习-回归）：
本问题为回归预测任务，需输出0-100%的连续数值。候选算法包括线性回归、决策树回归、随机森林回归。随机森林回归能够捕捉参数间的非线性关系，且与分类模型共用同一算法框架，便于统一管理与调用。评估指标采用均方根误差（RMSE）和决定系数（R²）。

数据预处理方案：
采用StandardScaler对四个原始特征进行标准化处理，消除量纲差异对模型的影响。同时构造交叉特征"切削强度=切削速度×切削深度"，增强模型对参数交互作用的捕捉能力。

2.3 与其他方案对比

与纯阈值规则方案对比：纯阈值方案技术点单一（仅规则判断），无法覆盖课程要求的3个技术方向；本方案涵盖特征工程、分类、回归、可视化4个方向，满足课程要求。

与深度学习方法对比：深度学习方案需要大量数据且训练时间长，模型可解释性差，不适合课程设计的时间与资源约束；本方案使用随机森林，数据需求低、训练快、可解释性强。

综上所述，本方案在满足课程技术要求的前提下，兼顾了开发可行性与系统效果，是适合本课程设计的最佳选择。

三、方案规划

3.1 技术栈

前端：HTML5 + CSS3 + JavaScript + ECharts 5
后端：Python 3.9 + Flask 2.0
数据库：SQLite 3
算法库：scikit-learn 1.2、pandas 1.5、numpy 1.23、joblib 1.2
版本控制：Git + GitHub
AI开发工具：通义灵码 + Qwen 3.8

3.2 需配置的外部资源

（1）Python运行环境：Python 3.9及以上版本，配置虚拟环境（venv或conda）。
（2）Python依赖包：flask、scikit-learn、pandas、numpy、joblib，通过pip install安装。
（3）前端图表库：ECharts 5，通过CDN在线引入（https://cdn.jsdelivr.net/npm/echarts@5/dist/echarts.min.js）。
（4）代码编辑器：VS Code，安装通义灵码插件用于AI辅助编程。
（5）版本控制：Git，关联GitHub远程仓库。

3.3 数据集来源与构建方案

本系统采用模拟生成方式构建数据集，不依赖外部公开数据源。使用Python的numpy库生成刀具切削参数与磨损状态数据，数据生成逻辑参照真实工艺原理设计。

数据集字段设计如下：
字段名cutting_speed，含义切削速度，单位m/min，取值范围80-220，与磨损关系为越大磨损越快。
字段名feed_rate，含义进给量，单位mm/r，取值范围0.05-0.35，与磨损关系为越大磨损越快。
字段名cutting_depth，含义切削深度，单位mm，取值范围0.5-4.0，与磨损关系为越大磨损越快。
字段名vibration，含义振动值，单位mm/s，取值范围0.1-2.0，与磨损关系为越大磨损越严重。
字段名tool_wear_level，含义磨损等级（标签），取值0-轻度、1-中度、2-重度，为目标变量。
字段名remaining_life，含义剩余寿命百分比（标签），取值范围0-100，为目标变量。

数据生成逻辑如下：
首先对四个原始特征进行归一化处理映射至0-1区间，然后计算磨损指数 = 0.3×速度归一化值 + 0.3×进给归一化值 + 0.3×深度归一化值 + 0.1×振动归一化值 + 随机噪声项。根据磨损指数划分等级：小于0.35为轻度磨损（0），0.35至0.65之间为中度磨损（1），大于0.65为重度磨损（2）。剩余寿命 = 100 - 磨损指数 × 100 + 随机波动项，并截断至0-100区间。

数据规模：共生成2000条样本，按7:3比例划分为训练集（1400条）和测试集（600条）。

3.4 前端UI方案

系统前端包含一个主页面，页面结构如下：

顶部导航栏：显示系统名称"刀具磨损程度智能评估系统"，右侧显示当前时间。

参数输入区：位于页面上方，包含四个数值输入框（切削速度、进给量、切削深度、振动值），每个输入框配有单位标注和取值范围提示；右侧设置"开始评估"按钮，点击后触发预测请求。

评估结果区：位于页面中部，以卡片形式展示三项评估结果——磨损等级（以不同颜色徽章显示轻度/中度/重度）、剩余寿命（以进度条形式显示百分比数值）、换刀建议（以绿/红灯形式显示"可继续使用"或"建议换刀"）。

图表展示区：位于页面下方，采用三列布局，从左至右依次为磨损等级分布饼图、剩余寿命趋势折线图、工艺参数雷达图。

历史记录区：位于页面底部，以表格形式展示最近10条评估记录，包含时间、四个输入参数、磨损等级、剩余寿命、换刀建议等列。

配色方案采用深蓝色（#1a3a5c）为主色调，搭配灰色（#f0f2f5）背景，符合工业软件简洁专业的视觉风格。

3.5 后端与算法方案

后端基于Flask框架开发，提供以下RESTful API接口：

接口/api/predict，方法POST，功能为刀具磨损评估，输入参数为cutting_speed、feed_rate、cutting_depth、vibration四个数值，输出结果为wear_level（0/1/2）、remaining_life（0-100数值）、suggestion（建议换刀/可继续使用）。

接口/api/history，方法GET，功能为获取历史评估记录，输入参数为limit（可选，默认10），输出为历史记录列表（含时间、参数、结果）。

接口/api/stats，方法GET，功能为获取统计数据，无输入参数，输出为各等级数量、平均剩余寿命等统计信息。


算法模块核心预测流程如下：
第一步，特征构造：将输入的四个原始特征与构造的交叉特征"切削强度=切削速度×切削深度"组合为五维特征向量。
第二步，标准化：使用训练时拟合好的StandardScaler对五维特征进行标准化处理。
第三步，分类预测：加载classifier.pkl模型，调用predict方法预测磨损等级（0/1/2）。
第四步，回归预测：加载regressor.pkl模型，调用predict方法预测剩余寿命百分比。
第五步，换刀建议：若磨损等级为2（重度）或剩余寿命低于30%，返回"建议换刀"，否则返回"可继续使用"。
第六步，数据库存储：将本次输入的参数、预测结果、建议及当前时间写入SQLite数据库。
第七步，返回JSON格式结果给前端。

3.6 系统架构图

本系统采用经典的三层B/S架构，自上而下为：

第一层表示层（前端浏览器）：包含参数输入表单、结果展示区、ECharts图表展示区，负责与用户交互和数据可视化展示，通过HTTP协议调用后端API接口。

第二层业务逻辑层（后端Flask服务）：包含路由层（接收并解析HTTP请求）、业务逻辑层（调用算法模块完成预测计算）、数据访问层（读写数据库）。算法模块进一步拆分为分类模型（随机森林多分类）、回归模型（随机森林回归）和数据预处理模块（标准化+特征构造）。

第三层数据层：左侧为SQLite数据库，存储预测记录表（包含id、timestamp、cutting_speed、feed_rate、cutting_depth、vibration、wear_level、remaining_life、suggestion等字段）；右侧为模型文件存储区，存放classifier.pkl、regressor.pkl、scaler.pkl三个持久化文件。

三层之间数据流向为：用户在前端输入参数 → 前端发送POST请求至后端/api/predict → 后端加载模型进行预测计算 → 后端读写数据库存储记录 → 后端返回JSON结果至前端 → 前端更新结果展示区并刷新图表。

四、技术路线

阶段1：数据准备
编写模拟数据生成脚本，生成2000条刀具切削参数与磨损状态数据，进行数据探索与可视化分析，按7:3比例划分训练集与测试集。产出物为模拟数据集文件。

阶段2：模型训练
对数据进行标准化预处理，构造交叉特征，训练随机森林分类模型与随机森林回归模型，使用测试集评估模型性能（准确率、F1-Score、RMSE、R²），将训练好的模型与标准化器持久化为.pkl文件。产出物为三个.pkl模型文件。

阶段3：后端开发
搭建Flask项目框架，编写模型加载与预测函数，实现/api/predict、/api/history、/api/stats三个API接口，封装数据库操作（建表、插入、查询）。产出物为完整后端服务代码。

阶段4：前端开发
编写HTML页面结构与CSS样式，实现参数输入表单与交互逻辑，集成ECharts图表（饼图、折线图、雷达图），编写前端JavaScript实现表单提交、结果展示、图表渲染，完成前后端联调测试。产出物为可交互的前端页面。

阶段5：集成与测试
进行系统全流程集成测试，编写自动化测试脚本验证API接口功能，修复Bug并优化界面交互细节，准备系统演示环境。产出物为可现场演示的完整系统。

